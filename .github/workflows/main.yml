name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Log in to DockerHub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker Image
      run: docker build -t ${{ secrets.DOCKER_USERNAME }}/blog:latest .
      
    - name: Push Docker Image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/blog:latest
      
    - name: Build MySQL Docker Image
      run: docker build -t ${{ secrets.DOCKER_USERNAME }}/mysql:latest -f Dockerfile.mysql .

    - name: Push MySQL Docker Image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/mysql:latest
